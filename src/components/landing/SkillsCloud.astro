<script>
    const mySkills = [
        "Python",
        "SQL",
        "Data Engineering",
        "Microservice Architecture",
        "JS/Node",
        "AWS",
        "Azure",
        "ETL",
        "Bootstrap",
        "Tailwind",
        "Tableau",
        "Data Analysis",
        "Application Development",
        "GraphQL",
        "Solutions Consulting",
        "Marketo",
        "Microsoft Power Platform",
        "Logic Apps",
        "Serverless Functions"
    ];

    const skillElements = mySkills.map((skill) => {
        const span = document.createElement("span");
        span.className = "w-auto m-1 rounded-full border-2 border-black bg-[#bc95d4] px-3 py-1.5 text-sm font-bold shadow-[4px_4px_0px_0px_rgba(0,0,0,1)] transition-all hover:translate-x-[3px] hover:translate-y-[3px] hover:shadow-none";
        span.textContent = skill;
        return span;
    });

    // Now, you can append these elements to a container in your HTML, for example:
    const container = document.getElementById("skillsContainer"); // Replace 'skillsContainer' with the actual ID of your container
    skillElements.forEach((element) => {
        container.appendChild(element);
    });
</script>

<div
    class="ring-1 lg:row-start-3 items-center h-full flex p-8 flex-row justify-center hover:ring-primary/5 dark:hover:ring-white/20 dark:ring-white/10 ring-primary/5 relative rounded-3xl overflow-hidden bg-white dark:bg-secondary shadow-xl dark:shadow-thick text-center"
>
    <div>
        <p
            class="text-xl tracking-tight font-bold text-primary dark:text-white md:text-6xl mt-6 mb-6 "
        >
            Skills
        </p>
        <div id="skillsContainer" class="mx-auto flex flex-wrap w-80"></div>
    </div>
</div>
